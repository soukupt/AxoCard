export function App() {
  return (
    <main className="app-shell">
      <section className="implementation-status" aria-label="AxoCard implementation">
        <h1>AxoCard</h1>
        <p>MASTER v2 implementation baseline.</p>
        <p className="muted">
          Production UI is intentionally not exposed until the Figma visual gate passes.
        </p>
      </section>
    </main>
  );
}
